#include "Costs.h"

float * onlabor::Cost::Quadratic(float * out, float * expected)
{
	return nullptr;
}

float * onlabor::Cost::Quadratic_D(float * result, float * out, float * expected, int size)
{
	//CPUSzorzo::SubtractMtx(result, out, expected, size);
	//CPUSzorzo::Szorzas(result, result, size, 1, )
	return nullptr;
}
